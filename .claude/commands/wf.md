# Implement a wireframe follow the user prompt
Follow the `Instructions` to implement the `WIREFRAMES` then `Report` the completed work.

## Instructions
- IMPORTANT: Create a wireframe as .md format base on user prompt.
- Create the wireframe in the `wf_specs/` directory with filename: `wf-{adw_id}-{descriptive-name}.md`
- Check for available **Skills** that could assist with the implementation and use them if appropriate.

## Plan
adw_id: $1
prompt: $2

## Report
- Summarize the work you've just done in a concise bullet point list.
- Report the files and total lines changed with `git diff --stat`
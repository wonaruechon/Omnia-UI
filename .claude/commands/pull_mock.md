# Fetch data from a user provied url
Follow the `Instructions` to fetch the data from the `url` then `Report` the completed work.

## Instructions
- IMPORTANT: Browse `url` using playwright mcp then waiting a user for logging in.
- Scroll from top to bottom of the entire page
    - Lazy-loaded content that appears on scroll. 
- Go through all sections of the `url` that can be expanded and clickable through expandable sections and accordions to reveal complete nested data.                                                
- Go through all clickable elements (buttons, links, interactive controls) for sample 'promotion', 'coupon',links.            
- Capture all content you have found.
- Create a data spec as .md file include all the fields you found on provided url.
- Save the data spec in the `mock_specs/` directory with filename: `mock-{adw_id}-{descriptive-name}.md`.

## Plan
adw_id: $1
url: $2

## Report
- Summarize the work you've just done in a concise bullet point list.

## Plan
adw_id: $1
url: $2

## Report
- Summarize all the fields you've just found in a concise bullet point list.